# 样式分层规范

## 分层定义
- `base/`：字体、变量、reset、全局基础。
- `layout/`：页面骨架布局（侧栏、主区、网格）。
- `components/`：组件外观（卡片、按钮、标签、气泡）。
- `utilities/`：高频工具类与动画助手。
- `themes/`：主题色与品牌配置。

## 约束
- 不跨层写样式：`layout` 不定义组件皮肤，`components` 不写页面级定位。
- 先复用再新增：优先复用已存在样式变量与工具类。
