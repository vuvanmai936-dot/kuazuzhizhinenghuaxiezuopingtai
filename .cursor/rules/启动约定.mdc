---
description: 启动命令默认同时启动网页与桌面App
alwaysApply: true
---

# 启动约定

当用户说“启动项目”或“启动”时，默认执行双模式启动：

- 网页模式：启动 Vite 开发服务（保留局域网访问能力）。
- 桌面模式：启动 Tauri 开发应用（与当前前端联动）。

执行要求：

- 不再要求用户重复说明“网页/桌面都要启动”。
- 若任一模式已在运行，先复用现有进程，避免重复拉起。
- 启动完成后，回复两个访问/运行结果（网页地址 + 桌面应用状态）。
