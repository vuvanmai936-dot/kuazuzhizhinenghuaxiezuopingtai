# 前端开发派工单：工程化迁移四期（抽屉模板特性化 + 回归清单）

## 输入来源
- `@docs/工单/17_0323_工程化迁移三期_前端派工单.md`
- `@src/scripts/ticket-drawer.js`

## 本期目标
- 继续降低脚本耦合：将抽屉模板迁移到 `features` 目录统一管理。
- 建立最小浏览器回归检查清单，支持每轮迭代快速验收。

## 开发范围
1. 在 `src/features/scenario-execution-layer/` 增加抽屉模板函数。
2. 改造 `src/scripts/ticket-drawer.js`，仅保留状态、事件与倒计时逻辑。
3. 补充回归检查清单文档（覆盖切换、抽屉、倒计时、场景触发、样式稳定性）。

## 验收标准
- 抽屉模板渲染依赖正确，不影响当前交互。
- `ticket-drawer.js` 职责更聚焦（模板逻辑迁出）。
- 回归清单可直接被 TPM 用于验收。

派工单已保存至：`docs/工单/20_0323_工程化迁移四期_前端派工单.md`，请前端开发 @ 该文件执行。  
为了维护一号位的资产时间线，已按序号规范落盘至 `docs/工单/20_0323_工程化迁移四期_前端派工单.md`。
