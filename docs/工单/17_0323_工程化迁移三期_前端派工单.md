# 前端开发派工单：工程化迁移三期（场景模板模块化）

## 输入来源
- `@docs/工单/14_0323_工程化迁移二期_前端派工单.md`
- `@src/scripts/message-renderer.js`

## 本期目标
- 将场景二/三的大段模板从渲染器文件中拆分至 `features` 目录，提升可维护性与复用性。

## 开发范围
1. 新增 `src/features/scenario-execution-layer/templates.js`
   - 承载执行层卡片模板与场景二模板片段。
2. 新增 `src/features/scenario-execution-layer/index.js`
   - 暴露模板函数到全局（阶段性兼容）。
3. 改造 `src/scripts/message-renderer.js`
   - 仅保留渲染流程与时序逻辑，不保留超长模板字符串。
4. 更新 `src/pages/im-main.html` 脚本加载顺序，先加载 features 再加载 renderer。

## 验收标准
- 功能行为与二期一致（切换/卡片/抽屉/倒计时）。
- `message-renderer.js` 体积明显下降。
- 浏览器直接打开 `src/pages/im-main.html` 可运行。

派工单已保存至：`docs/工单/17_0323_工程化迁移三期_前端派工单.md`，请前端开发 @ 该文件执行。  
为了维护一号位的资产时间线，已按序号规范落盘至 `docs/工单/17_0323_工程化迁移三期_前端派工单.md`。
