# Bug/遗漏修复清单：执行层场景

## 来源
- `@docs/工单/02_0323_执行层场景_前端派工单.md`

## 修复项
1. 会话切换状态高亮需与当前群组同步，避免多项同时高亮。
2. 卡片中的“组长可见按钮”需有权限态样式区分（可见但强调权限属性）。
3. 切换动画结束后滚动位置需自动定位到最新消息。
4. 卡片内“倒计时2小时”文案需保持稳定展示，不随刷新丢失。

## 回归验证
- 执行层与总控层来回切换 5 次，内容与样式保持正确。
- 不出现报错、不出现重复消息堆叠。

## 修复通过标准
- 以上四项全部通过后方可 Close。

为了维护一号位的资产时间线，已按序号规范落盘至 `docs/工单/03_0323_执行层场景_Bug修复清单.md`。
