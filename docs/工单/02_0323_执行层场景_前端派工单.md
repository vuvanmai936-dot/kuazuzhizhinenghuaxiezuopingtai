# 前端开发派工单：执行层场景

## 输入来源
- `@docs/战略交接单/01_0323_跨组织协同_战略交接单.md`

## 开发目标
- 完成执行层会话切换、异常上报消息、执行辅助智能体响应卡片。
- 维持飞书风格 UI 与单文件可运行特性。

## 实施范围
- 左侧会话点击切换为执行层上下文。
- Header 更新为执行层标题与状态标签。
- 消息区渲染“应用方-张三”异常上报。
- 卡片展示异常分级、横向匹配、处置状态及两个操作按钮。

## 非功能要求
- 切换动画平滑，无明显闪烁。
- 函数需有注释，结构清晰。
- 禁止引入本地依赖或后端请求。

## QA 验收标准
- 点击“子项目A-集成协同群组”后 300ms 内完成视图切换。
- 卡片内容字段完整，按钮文案与数量准确。
- 切回总控群后，原场景一/二消息可恢复。

## 交付要求
- 代码落盘后在回复中给出文件路径。
- 说明实现摘要与待优化项。

为了维护一号位的资产时间线，已按序号规范落盘至 `docs/工单/02_0323_执行层场景_前端派工单.md`。
